# TODO

## バグ修正

- [ ] 同時操作で同期が不安定なときがある（片側にしか反映されないと報告あり）。同期ボタンをつけて手動で過去のレコードを再取得できるようにすれば解決しそう。

## UX改善

- [ ] 同名メンバーの識別 → 連番付与（Nostr追加は対象外）例: 田中、田中(2)、田中(3)
- [ ] マイナス金額の入力禁止 → `min="1"` 追加
- [ ] 長い名前の制限 → 最大30文字
- [ ] 支出追加後に追加したレコードが下の方にあり追加されたことがわかりにくい。レコードの順序を上を新しくするか、追加レコードまでスクロールさせる。（追加しました。みたいなダイアログでもいいかも）
